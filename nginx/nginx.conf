user nginx;
worker_processes auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log main;

    sendfile            on;
    keepalive_timeout   65;
    client_max_body_size 100M;

    upstream django_server {
        server web:8000;
    }

    # Redirect HTTP to HTTPS
    server {
        listen 80;
        server_name glasshalf.ru www.glasshalf.ru;

        # Для Certbot
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            try_files $uri =404;
        }

        return 301 https://$host$request_uri;
    }

    # Основной HTTPS‑сервер
    server {
        listen 443 ssl;
        http2 on;
        server_name glasshalf.ru www.glasshalf.ru;

        # SSL Certificates
        ssl_certificate     /etc/letsencrypt/live/glasshalf.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/glasshalf.ru/privkey.pem;

        # Modern SSL settings
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 1d;
        ssl_session_tickets off;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers on;

        # HSTS и security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff";
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-XSS-Protection "1; mode=block";
        add_header Content-Security-Policy "default-src 'self'";

        # Статика
        location /static/ {
            alias /code/static/;
            expires 365d;
            access_log off;
            add_header Cache-Control "public";
        }

        # Медиа
        location /media/ {
            alias /code/media/;
            expires 30d;
            access_log off;
            add_header Cache-Control "public";
        }

        # Django‑приложение
        location / {
            proxy_pass         http://django_server;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_redirect     off;
            proxy_buffering    off;
        }

        # Страницы ошибок
        error_page 500 502 503 504 /500.html;
        location = /500.html {
            root /code/static/;
        }
    }
}


# user  nginx;
# worker_processes  auto;

# error_log  /var/log/nginx/error.log warn;
# pid        /var/run/nginx.pid;

# events {
#     worker_connections 1024;
# }

# http {
#     include       /etc/nginx/mime.types;
#     default_type  application/octet-stream;

#     access_log  /var/log/nginx/access.log;

#     sendfile        on;
#     keepalive_timeout  65;

#     # 👇 вот здесь можно вставлять server
#     server {
#         listen 80;
#         server_name web:8000;

#         location / {
#             return 404;
#         }
#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#             try_files $uri $uri/ =404;
# }
#     }
# }

# worker_processes auto;

# events {
#     worker_connections 1024;
# }

# http {
#     include /etc/nginx/mime.types;
#     default_type application/octet-stream;
#     sendfile on;
#     keepalive_timeout 65;

#     # Редирект HTTP на HTTPS
#     server {
#         listen 80;
#         server_name glasshalf.ru www.glasshalf.ru;
#         server_tokens off;

#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#         }

#         location / {
#             return 301 https://$host$request_uri;
#         }
#     }

#     # HTTPS сервер
#     server {
#         listen 443 ssl;
#         server_name glasshalf.ru www.glasshalf.ru;
#         server_tokens off;

#         ssl_certificate /etc/letsencrypt/live/glasshalf.ru/fullchain.pem;
#         ssl_certificate_key /etc/letsencrypt/live/glasshalf.ru/privkey.pem;
#         # include /etc/nginx/options-ssl-nginx.conf;
#         # ssl_dhparam /etc/nginx/ssl-dhparams.pem;

#         # Статические файлы Django
#         location /static/ {
#             autoindex off;
#             alias /code/staticfiles/;
#             expires 1y;
#             add_header Cache-Control "public";
#         }

#         # Обработка медиа-файлов (если нужно)
#         # location /media/ {
#         #     alias /code/media/;
#         # }

#         # Основные запросы проксируем в Django
#         location / {
#             proxy_pass http://web:8000;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }

#         # Если нужно добавить обработку для бота
#         # location /bot/ {
#         #     proxy_pass http://bot:3000;  # Пример порта
#         #     ...аналогичные настройки прокси
#         # }
#     }
# }
